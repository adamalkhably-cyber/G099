# ── Flask ──────────────────────────────────────────────────────────────
SECRET_KEY=replace-me-with-a-strong-random-secret
DEBUG=False

# ── Database ────────────────────────────────────────────────────────────
# SQLite (dev default):
DATABASE_URL=sqlite:///digital_closet.db

# PostgreSQL (production example):
# DATABASE_URL=postgresql://user:password@localhost:5432/digital_closet

# ── JWT ─────────────────────────────────────────────────────────────────
JWT_SECRET_KEY=replace-me-with-another-strong-secret
